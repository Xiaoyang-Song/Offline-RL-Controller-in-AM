"""
baselines/proportional
-------------------------
Baseline 2: proportional (P) controller. A single data-informed gain,
fit once via ordinary least squares (no gradient descent, no RL) against
the offline dataset — a classical, non-learning process-control baseline.
"""
